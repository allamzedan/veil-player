import { VEIL_ACTION_COLORS, type VeilActionKind } from './veilDesignTokens'

/** Semantic mask/mute/skip colors for Inspector and timeline (matches --veil-mask/mute/skip). */
export const INSPECTOR_ACTION_COLORS = VEIL_ACTION_COLORS

export type InspectorActionKind = VeilActionKind
